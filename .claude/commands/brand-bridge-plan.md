---
description: Main-site ↔ portal cross-linking plan — CTA map, trust links, copy examples, no-sales-pressure bridge language, and which pages to update.
---

Run the brand bridge plan.

1. **portal-brand-bridge-editor** — draft the main-site→portal and portal→main-site bridge copy, chapter-by-chapter and page-by-page.
2. **content-architect** — confirm which chapters/books can structurally carry a portal CTA without disrupting the content model, and where a CTA slot would need a schema field that doesn't exist yet.
3. **seo-research-strategist** — confirm the bridge doesn't cannibalize or conflict with existing chapter search intent.
4. This repo doesn't have a dedicated `onpage-seo-editor` agent — fold that review into `seo-research-strategist`'s pass above rather than assuming a separate agent exists.

Rules: soft bridge only, never sales pressure. No page implies Local Connect has more scale or maturity than it actually has. Drafts only — nothing is published without founder approval.

Return:
- main site → portal CTA map (chapter/book → CTA line → destination portal page)
- portal → main site trust links
- copy examples for each direction
- pages to update, ranked by traffic/relevance

$ARGUMENTS
