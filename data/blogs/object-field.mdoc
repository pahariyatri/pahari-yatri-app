---
title: Object field
description: >-
  The object field is used to create complex object schemas that can contain any
  other fields.


  It's particularly useful when you need a set of fields for each option in an
  array field, conditional field or blocks field.
image: static/images/blogs/object-field/image.jpg
---
The `object` field is used to create complex object schemas that can contain any other fields.

It's particularly useful when you need a set of fields for each option in an [**array field**](https://keystatic.com/docs/fields/array), [**conditional field**](https://keystatic.com/docs/fields/conditional) or [**blocks field**](https://keystatic.com/docs/fields/blocks).The `object` field is used to create complex object schemas that can contain any other fields.

It's particularly useful when you need a set of fields for each option in an [**array field**](https://keystatic.com/docs/fields/array), [**conditional field**](https://keystatic.com/docs/fields/conditional) or [**blocks field**](https://keystatic.com/docs/fields/blocks).The `object` field is used to create complex object schemas that can contain any other fields.

It's particularly useful when you need a set of fields for each option in an [**array field**](https://keystatic.com/docs/fields/array), [**conditional field**](https://keystatic.com/docs/fields/conditional) or [**blocks field**](https://keystatic.com/docs/fields/blocks).
